#include <iostream>
#include <algorithm>
#include <queue>

using namespace std;

int r, c, result;
int dx[] = { -1,1, 0,0 };
int dy[] = { 0,0,1,-1 };

int graph[1001][1001];
bool vis[1001][1001];

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	char input = '\0';
	for (int i = 0; i < c; i++) {
		for (int j = 0; j < r; j++) {
			
		}
	}
}